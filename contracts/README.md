# Contracts

Files shared between programs that are deployed separately and can therefore be out of
step: the hub on the Raspberry Pi 5, and each satellite on its own board. Everything here
is generated from, or tested against, code in `src/`, and versioned by directory.
